version: 4.0.70
steps:
  - prompt: Click on "Login" in the header.
    commands:
      - command: hover-text
        text: Login
        description: login button in the header
        action: click
  - prompt: Assert that the login page displays correctly.
    commands:
      - command: assert
        expect: the login page displays correctly
  - prompt: >-
      Navigate back to the homepage.  # Use the content of the file from the
      environment
    commands:
      - command: press-keys
        keys:
          - alt
          - left
  - prompt: >-
      Navigate back to the homepage.  # Use the content of the file from the
      environment
    commands:
      - command: press-keys
        keys:
          - alt
          - left
  - prompt: >-
      Navigate back to the homepage.  # Use the content of the file from the
      environment
    commands:
      - command: press-keys
        keys:
          - alt
          - left
  - prompt: >-
      Navigate back to the homepage.  # Use the content of the file from the
      environment
    commands:
      - command: press-keys
        keys:
          - alt
          - left
  - prompt: >-
      Navigate back to the homepage.  # Use the content of the file from the
      environment
    commands:
      - command: press-keys
        keys:
          - alt
          - left
  - prompt: >-
      Navigate back to the homepage.  # Use the content of the file from the
      environment
    commands:
      - command: press-keys
        keys:
          - alt
          - left
  - prompt: >-
      Navigate back to the homepage.  # Use the content of the file from the
      environment
    commands:
      - command: press-keys
        keys:
          - alt
          - left
  - prompt: >-
      Navigate back to the homepage.  # Use the content of the file from the
      environment
    commands:
      - command: hover-text
        text: vendorpm
        description: logo in the header
        action: click
  - prompt: >-
      Navigate back to the homepage.  # Use the content of the file from the
      environment
    commands:
      - command: press-keys
        keys:
          - alt
          - left
  - prompt: >-
      Navigate back to the homepage.  # Use the content of the file from the
      environment
    commands:
      - command: press-keys
        keys:
          - alt
          - left
